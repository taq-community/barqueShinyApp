% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primers.R
\name{get_primer_names}
\alias{get_primer_names}
\title{Extract Primer Names from a Hashtag-Commented CSV File}
\usage{
get_primer_names(input_file = get_golem_config("bq_primer_file"))
}
\arguments{
\item{input_file}{Character. Path to the CSV file containing primer definitions, with rows optionally
prefixed by `#`.}
}
\value{
A character vector of primer names (first column), with any leading `#` removed.
}
\description{
Parses a CSV-style primer definition file in which each row may be commented out with a leading `#`,
and extracts the names from the first column of all valid lines. The leading `#` is removed from each entry.
}
\details{
This function is useful when reading primer configuration files where all entries may be commented
out by default. It extracts all lines that look like primer entries (i.e., match a pattern like `#PrimerName,...`)
and strips the comment symbol from the primer name.
}
\examples{
\dontrun{
primers <- get_primer_names("primers.csv")
print(primers)
}

}
